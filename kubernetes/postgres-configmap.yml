apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  labels:
    app: postgres
data:
  POSTGRES_DB: postgresdb
  POSTGRES_USER: jmmpahcoe
  POSTGRES_PASSWORD: avocado
  PGDATA: /var/lib/postgresql/data/pgdata
